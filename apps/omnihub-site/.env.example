# ============================================================
# APEX OmniHub — Marketing Site Environment Variables
# Copy to .env.local for local dev. Set in Vercel → Settings
# → Environment Variables for production.
# NEVER commit a file with real values.
# ============================================================

# Supabase project URL
# Get from: https://app.supabase.com/project/_/settings/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase public (anon) key — use EITHER name; both are accepted.
# "Publishable key" is the canonical name used in .env.example at repo root.
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here
# VITE_SUPABASE_ANON_KEY=your-anon-key-here  # legacy alias — also works

# URL of the main app dashboard after login
# Defaults to /omnidash if not set.
VITE_DASHBOARD_URL=https://app.apexomnihub.icu
