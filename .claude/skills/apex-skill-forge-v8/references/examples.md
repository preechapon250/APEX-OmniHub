# APEX Skill Forge — Archetype Examples

## Workflow Example

```bash
python scripts/forge.py invoice-processor --arch workflow --path ./skills
```

Creates a sequential multi-step processor with validation gates.

## Toolkit Example

```bash
python scripts/forge.py data-utils --arch toolkit --path ./skills
```

Creates a multi-function capability index.

## Domain Example

```bash
python scripts/forge.py tax-advisor --arch domain --path ./skills
```

Creates an expert decision-tree system.

## Orchestrator Example

```bash
python scripts/forge.py pipeline-coordinator --arch orchestrator --path ./skills
```

Creates an event-driven skill coordinator.

## Transformer Example

```bash
python scripts/forge.py csv-to-json --arch transformer --path ./skills
```

Creates a data format conversion pipeline.

## Guardian Example

```bash
python scripts/forge.py input-validator --arch guardian --path ./skills
```

Creates a validation/security rules engine.

---

Copyright (c) 2026 APEX Business Systems Ltd. All Rights Reserved.
