# Patterns Library (Universal)

- Onboarding: value first, permissions later
- Search: suggestions + empty/no-results states
- Lists: virtualization, retry on pagination failure
- Forms: persistent labels, inline validation, correct keyboard types
- Checkout: clear summary, no surprise fees, receipt + next steps
- Recovery: inline errors, preserve input, undo where feasible
