Extract this nested ternary operation into an independent statement.

Ternary operators should not be nested typescript:S3358
Software qualities impacted:
Maintainability


2
Medium
Open
Not assigned
Code Smell
Major
Tags
confusing
+
Line affected
L34
Effort
5 min
Introduced
38 minutes ago
Where is the issue?
Why is this an issue?
Activity
More info

Open in IDE
src/components/AppTile.tsx


See all issues in this file
New code
import { Download } from 'lucide-react';
export interface AppData {
    name: string;
    icon?: string;
    alt?: string;
    url?: string;
    path?: string;
}
interface AppTileProps {
    app: AppData;
    isInstallable: boolean;
    onInstall: () => void;
    onNavigate: (path: string) => void;
    onOpenUrl: (url: string) => void;
    variant?: 'small' | 'large';
}
export const AppTile = ({
    app,
    isInstallable,
    onInstall,
    onNavigate,
    onOpenUrl,
    variant = 'small'
}: AppTileProps) => {
    const isSmall = variant === 'small';
    const containerClasses = [
        'aspect-square rounded-xl flex flex-col items-center justify-center text-center transition-transform hover:scale-105 cursor-pointer',
        isSmall ? 'p-4' : 'p-6',
        app.icon
            ? `bg-white border-2 border-[hsl(var(--navy))] shadow-lg ${!isSmall ? 'hover:shadow-xl' : ''}`
Extract this nested ternary operation into an independent statement.

            : 'bg-white border-2 border-dashed border-muted-foreground/30'
    ].join(' ');
    const iconClasses = [
        'mb-2 object-cover rounded-lg',
        isSmall ? 'w-icon-sm h-icon-sm md:w-icon-md md:h-icon-md' : 'w-icon-lg h-icon-lg md:w-icon-xl md:h-icon-xl mb-3'
    ].join(' ');
    const textClasses = [
        'font-medium text-[hsl(var(--navy))]',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-semibold'
    ].join(' ');
    const placeholderClasses = [
        'text-muted-foreground',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-medium'
    ].join(' ');
    const badgeClasses = [
        'absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse',
        'p-1 -top-1 -right-1', // Small variant defaults (adjust if large is different)
    ].join(' ');
    const handleClick = () => {
        if (app.name === 'APEX' && isInstallable) {
            onInstall();
        } else if (app.url) {
            onOpenUrl(app.url);
        } else if (app.path) {
            onNavigate(app.path);
        }
    };
    return (
        <div
            role="group"
            aria-label={`${app.name} tile`}
            className={containerClasses}
            onClick={handleClick}
        >
            {app.icon ? (
                <>
                    <div className="relative">
                        <img
                            src={app.icon}
                            alt={app.alt}
                            className={iconClasses}
                        />
                        {app.name === 'APEX' && isInstallable && (
                            <div className={`absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse ${isSmall ? '-top-1 -right-1 p-1' : '-top-2 -right-2 p-2'}`}>
                                <Download className={isSmall ? "w-3 h-3" : "w-4 h-4 md:w-5 md:h-5"} />
                            </div>
                        )}
                    </div>
                    <span className={textClasses}>
                        {app.name}
                    </span>
                </>
            ) : (
                <span className={placeholderClasses}>
                    {app.name}
                </span>
            )}
        </div>
    );
};




============================================================================================================================================================================================





Unexpected negated condition.

Negated conditions should be avoided when an else clause is present typescript:S7735
Software qualities impacted:
Maintainability


3
Low
Open
Not assigned
Code Smell
Minor
Tags
readability
+
Line affected
L34
Effort
2 min
Introduced
38 minutes ago
Where is the issue?
Why is this an issue?
Activity

Open in IDE
src/components/AppTile.tsx


See all issues in this file
New code
import { Download } from 'lucide-react';
export interface AppData {
    name: string;
    icon?: string;
    alt?: string;
    url?: string;
    path?: string;
}
interface AppTileProps {
    app: AppData;
    isInstallable: boolean;
    onInstall: () => void;
    onNavigate: (path: string) => void;
    onOpenUrl: (url: string) => void;
    variant?: 'small' | 'large';
}
export const AppTile = ({
    app,
    isInstallable,
    onInstall,
    onNavigate,
    onOpenUrl,
    variant = 'small'
}: AppTileProps) => {
    const isSmall = variant === 'small';
    const containerClasses = [
        'aspect-square rounded-xl flex flex-col items-center justify-center text-center transition-transform hover:scale-105 cursor-pointer',
        isSmall ? 'p-4' : 'p-6',
        app.icon
            ? `bg-white border-2 border-[hsl(var(--navy))] shadow-lg ${!isSmall ? 'hover:shadow-xl' : ''}`
Unexpected negated condition.

            : 'bg-white border-2 border-dashed border-muted-foreground/30'
    ].join(' ');
    const iconClasses = [
        'mb-2 object-cover rounded-lg',
        isSmall ? 'w-icon-sm h-icon-sm md:w-icon-md md:h-icon-md' : 'w-icon-lg h-icon-lg md:w-icon-xl md:h-icon-xl mb-3'
    ].join(' ');
    const textClasses = [
        'font-medium text-[hsl(var(--navy))]',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-semibold'
    ].join(' ');
    const placeholderClasses = [
        'text-muted-foreground',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-medium'
    ].join(' ');
    const badgeClasses = [
        'absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse',
        'p-1 -top-1 -right-1', // Small variant defaults (adjust if large is different)
    ].join(' ');
    const handleClick = () => {
        if (app.name === 'APEX' && isInstallable) {
            onInstall();
        } else if (app.url) {
            onOpenUrl(app.url);
        } else if (app.path) {
            onNavigate(app.path);
        }
    };
    return (
        <div
            role="group"
            aria-label={`${app.name} tile`}
            className={containerClasses}
            onClick={handleClick}
        >
            {app.icon ? (
                <>
                    <div className="relative">
                        <img
                            src={app.icon}
                            alt={app.alt}
                            className={iconClasses}
                        />
                        {app.name === 'APEX' && isInstallable && (
                            <div className={`absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse ${isSmall ? '-top-1 -right-1 p-1' : '-top-2 -right-2 p-2'}`}>
                                <Download className={isSmall ? "w-3 h-3" : "w-4 h-4 md:w-5 md:h-5"} />
                            </div>
                        )}
                    </div>
                    <span className={textClasses}>
                        {app.name}
                    </span>
                </>
            ) : (
                <span className={placeholderClasses}>
                    {app.name}
                </span>
            )}
        </div>
    );
};






============================================================================================================================================================================================






Intentionality
Not logical
Remove this useless assignment to variable "badgeClasses".

Unused assignments should be removed typescript:S1854
Software qualities impacted:
Maintainability


2
Medium
Open
Not assigned
Code Smell
Major
Tags
cwe
...
+
Line affected
L53
Effort
1 min
Introduced
39 minutes ago
Where is the issue?
Why is this an issue?
How can I fix it?
Activity
More info

Open in IDE
src/components/AppTile.tsx


See all issues in this file
New code
import { Download } from 'lucide-react';
export interface AppData {
    name: string;
    icon?: string;
    alt?: string;
    url?: string;
    path?: string;
}
interface AppTileProps {
    app: AppData;
    isInstallable: boolean;
    onInstall: () => void;
    onNavigate: (path: string) => void;
    onOpenUrl: (url: string) => void;
    variant?: 'small' | 'large';
}
export const AppTile = ({
    app,
    isInstallable,
    onInstall,
    onNavigate,
    onOpenUrl,
    variant = 'small'
}: AppTileProps) => {
    const isSmall = variant === 'small';
    const containerClasses = [
        'aspect-square rounded-xl flex flex-col items-center justify-center text-center transition-transform hover:scale-105 cursor-pointer',
        isSmall ? 'p-4' : 'p-6',
        app.icon
            ? `bg-white border-2 border-[hsl(var(--navy))] shadow-lg ${!isSmall ? 'hover:shadow-xl' : ''}`
            : 'bg-white border-2 border-dashed border-muted-foreground/30'
    ].join(' ');
    const iconClasses = [
        'mb-2 object-cover rounded-lg',
        isSmall ? 'w-icon-sm h-icon-sm md:w-icon-md md:h-icon-md' : 'w-icon-lg h-icon-lg md:w-icon-xl md:h-icon-xl mb-3'
    ].join(' ');
    const textClasses = [
        'font-medium text-[hsl(var(--navy))]',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-semibold'
    ].join(' ');
    const placeholderClasses = [
        'text-muted-foreground',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-medium'
    ].join(' ');
    const badgeClasses = [
Remove this useless assignment to variable "badgeClasses".

        'absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse',
        'p-1 -top-1 -right-1', // Small variant defaults (adjust if large is different)
    ].join(' ');
    const handleClick = () => {
        if (app.name === 'APEX' && isInstallable) {
            onInstall();
        } else if (app.url) {
            onOpenUrl(app.url);
        } else if (app.path) {
            onNavigate(app.path);
        }
    };
    return (
        <div
            role="group"
            aria-label={`${app.name} tile`}
            className={containerClasses}
            onClick={handleClick}
        >
            {app.icon ? (
                <>
                    <div className="relative">
                        <img
                            src={app.icon}
                            alt={app.alt}
                            className={iconClasses}
                        />
                        {app.name === 'APEX' && isInstallable && (
                            <div className={`absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse ${isSmall ? '-top-1 -right-1 p-1' : '-top-2 -right-2 p-2'}`}>
                                <Download className={isSmall ? "w-3 h-3" : "w-4 h-4 md:w-5 md:h-5"} />
                            </div>
                        )}
                    </div>
                    <span className={textClasses}>
                        {app.name}
                    </span>
                </>
            ) : (
                <span className={placeholderClasses}>
                    {app.name}
                </span>
            )}
        </div>
    );
};





============================================================================================================================================================================================






Consistency
Not conventional
Non-interactive elements should not be assigned mouse or keyboard event listeners.

Non-interactive elements shouldn't have event handlers typescript:S6847
Software qualities impacted:
Reliability


3
Low
Maintainability


2
Medium
Open
Not assigned
Code Smell
Major
Tags
accessibility
...
+
Line affected
L74
Effort
5 min
Introduced
40 minutes ago
Where is the issue?
Why is this an issue?
How can I fix it?
Activity
More info

Open in IDE
src/components/AppTile.tsx


See all issues in this file
New code
import { Download } from 'lucide-react';
export interface AppData {
    name: string;
    icon?: string;
    alt?: string;
    url?: string;
    path?: string;
}
interface AppTileProps {
    app: AppData;
    isInstallable: boolean;
    onInstall: () => void;
    onNavigate: (path: string) => void;
    onOpenUrl: (url: string) => void;
    variant?: 'small' | 'large';
}
export const AppTile = ({
    app,
    isInstallable,
    onInstall,
    onNavigate,
    onOpenUrl,
    variant = 'small'
}: AppTileProps) => {
    const isSmall = variant === 'small';
    const containerClasses = [
        'aspect-square rounded-xl flex flex-col items-center justify-center text-center transition-transform hover:scale-105 cursor-pointer',
        isSmall ? 'p-4' : 'p-6',
        app.icon
            ? `bg-white border-2 border-[hsl(var(--navy))] shadow-lg ${!isSmall ? 'hover:shadow-xl' : ''}`
            : 'bg-white border-2 border-dashed border-muted-foreground/30'
    ].join(' ');
    const iconClasses = [
        'mb-2 object-cover rounded-lg',
        isSmall ? 'w-icon-sm h-icon-sm md:w-icon-md md:h-icon-md' : 'w-icon-lg h-icon-lg md:w-icon-xl md:h-icon-xl mb-3'
    ].join(' ');
    const textClasses = [
        'font-medium text-[hsl(var(--navy))]',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-semibold'
    ].join(' ');
    const placeholderClasses = [
        'text-muted-foreground',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-medium'
    ].join(' ');
    const badgeClasses = [
        'absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse',
        'p-1 -top-1 -right-1', // Small variant defaults (adjust if large is different)
    ].join(' ');
    const handleClick = () => {
        if (app.name === 'APEX' && isInstallable) {
            onInstall();
        } else if (app.url) {
            onOpenUrl(app.url);
        } else if (app.path) {
            onNavigate(app.path);
        }
    };
    return (
        <div
Non-interactive elements should not be assigned mouse or keyboard event listeners.

            role="group"
            aria-label={`${app.name} tile`}
            className={containerClasses}
            onClick={handleClick}
        >
            {app.icon ? (
                <>
                    <div className="relative">
                        <img
                            src={app.icon}
                            alt={app.alt}
                            className={iconClasses}
                        />
                        {app.name === 'APEX' && isInstallable && (
                            <div className={`absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse ${isSmall ? '-top-1 -right-1 p-1' : '-top-2 -right-2 p-2'}`}>
                                <Download className={isSmall ? "w-3 h-3" : "w-4 h-4 md:w-5 md:h-5"} />
                            </div>
                        )}
                    </div>
                    <span className={textClasses}>
                        {app.name}
                    </span>
                </>
            ) : (
                <span className={placeholderClasses}>
                    {app.name}
                </span>
            )}
        </div>
    );
};





============================================================================================================================================================================================





Consistency
Not conventional
Use <details>, <fieldset>, <optgroup>, or <address> instead of the "group" role to ensure accessibility across all devices.

Prefer tag over ARIA role typescript:S6819
Software qualities impacted:
Maintainability


2
Medium
Open
Not assigned
Code Smell
Major
Tags
accessibility
...
+
Line affected
L74
Effort
5 min
Introduced
40 minutes ago
Where is the issue?
Why is this an issue?
Activity
More info

Open in IDE
src/components/AppTile.tsx


See all issues in this file
New code
import { Download } from 'lucide-react';
export interface AppData {
    name: string;
    icon?: string;
    alt?: string;
    url?: string;
    path?: string;
}
interface AppTileProps {
    app: AppData;
    isInstallable: boolean;
    onInstall: () => void;
    onNavigate: (path: string) => void;
    onOpenUrl: (url: string) => void;
    variant?: 'small' | 'large';
}
export const AppTile = ({
    app,
    isInstallable,
    onInstall,
    onNavigate,
    onOpenUrl,
    variant = 'small'
}: AppTileProps) => {
    const isSmall = variant === 'small';
    const containerClasses = [
        'aspect-square rounded-xl flex flex-col items-center justify-center text-center transition-transform hover:scale-105 cursor-pointer',
        isSmall ? 'p-4' : 'p-6',
        app.icon
            ? `bg-white border-2 border-[hsl(var(--navy))] shadow-lg ${!isSmall ? 'hover:shadow-xl' : ''}`
            : 'bg-white border-2 border-dashed border-muted-foreground/30'
    ].join(' ');
    const iconClasses = [
        'mb-2 object-cover rounded-lg',
        isSmall ? 'w-icon-sm h-icon-sm md:w-icon-md md:h-icon-md' : 'w-icon-lg h-icon-lg md:w-icon-xl md:h-icon-xl mb-3'
    ].join(' ');
    const textClasses = [
        'font-medium text-[hsl(var(--navy))]',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-semibold'
    ].join(' ');
    const placeholderClasses = [
        'text-muted-foreground',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-medium'
    ].join(' ');
    const badgeClasses = [
        'absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse',
        'p-1 -top-1 -right-1', // Small variant defaults (adjust if large is different)
    ].join(' ');
    const handleClick = () => {
        if (app.name === 'APEX' && isInstallable) {
            onInstall();
        } else if (app.url) {
            onOpenUrl(app.url);
        } else if (app.path) {
            onNavigate(app.path);
        }
    };
    return (
        <div
Use <details>, <fieldset>, <optgroup>, or <address> instead of the "group" role to ensure accessibility across all devices.

            role="group"
            aria-label={`${app.name} tile`}
            className={containerClasses}
            onClick={handleClick}
        >
            {app.icon ? (
                <>
                    <div className="relative">
                        <img
                            src={app.icon}
                            alt={app.alt}
                            className={iconClasses}
                        />
                        {app.name === 'APEX' && isInstallable && (
                            <div className={`absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse ${isSmall ? '-top-1 -right-1 p-1' : '-top-2 -right-2 p-2'}`}>
                                <Download className={isSmall ? "w-3 h-3" : "w-4 h-4 md:w-5 md:h-5"} />
                            </div>
                        )}
                    </div>
                    <span className={textClasses}>
                        {app.name}
                    </span>
                </>
            ) : (
                <span className={placeholderClasses}>
                    {app.name}
                </span>
            )}
        </div>
    );
};





=============================================================================================================================================================================================






Visible, non-interactive elements with click handlers must have at least one keyboard listener.

Mouse events should have corresponding keyboard events typescript:S1082
Software qualities impacted:
Reliability


3
Low
Open
Not assigned
Bug
Minor
Tags
accessibility
...
+
Line affected
L74
Effort
5 min
Introduced
41 minutes ago
Where is the issue?
Why is this an issue?
How can I fix it?
Activity
More info

Open in IDE
src/components/AppTile.tsx


See all issues in this file
New code
import { Download } from 'lucide-react';
export interface AppData {
    name: string;
    icon?: string;
    alt?: string;
    url?: string;
    path?: string;
}
interface AppTileProps {
    app: AppData;
    isInstallable: boolean;
    onInstall: () => void;
    onNavigate: (path: string) => void;
    onOpenUrl: (url: string) => void;
    variant?: 'small' | 'large';
}
export const AppTile = ({
    app,
    isInstallable,
    onInstall,
    onNavigate,
    onOpenUrl,
    variant = 'small'
}: AppTileProps) => {
    const isSmall = variant === 'small';
    const containerClasses = [
        'aspect-square rounded-xl flex flex-col items-center justify-center text-center transition-transform hover:scale-105 cursor-pointer',
        isSmall ? 'p-4' : 'p-6',
        app.icon
            ? `bg-white border-2 border-[hsl(var(--navy))] shadow-lg ${!isSmall ? 'hover:shadow-xl' : ''}`
            : 'bg-white border-2 border-dashed border-muted-foreground/30'
    ].join(' ');
    const iconClasses = [
        'mb-2 object-cover rounded-lg',
        isSmall ? 'w-icon-sm h-icon-sm md:w-icon-md md:h-icon-md' : 'w-icon-lg h-icon-lg md:w-icon-xl md:h-icon-xl mb-3'
    ].join(' ');
    const textClasses = [
        'font-medium text-[hsl(var(--navy))]',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-semibold'
    ].join(' ');
    const placeholderClasses = [
        'text-muted-foreground',
        isSmall ? 'text-xs md:text-sm' : 'text-sm md:text-base font-medium'
    ].join(' ');
    const badgeClasses = [
        'absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse',
        'p-1 -top-1 -right-1', // Small variant defaults (adjust if large is different)
    ].join(' ');
    const handleClick = () => {
        if (app.name === 'APEX' && isInstallable) {
            onInstall();
        } else if (app.url) {
            onOpenUrl(app.url);
        } else if (app.path) {
            onNavigate(app.path);
        }
    };
    return (
        <div
Visible, non-interactive elements with click handlers must have at least one keyboard listener.

            role="group"
            aria-label={`${app.name} tile`}
            className={containerClasses}
            onClick={handleClick}
        >
            {app.icon ? (
                <>
                    <div className="relative">
                        <img
                            src={app.icon}
                            alt={app.alt}
                            className={iconClasses}
                        />
                        {app.name === 'APEX' && isInstallable && (
                            <div className={`absolute bg-[hsl(var(--navy))] text-white rounded-full shadow-lg animate-pulse ${isSmall ? '-top-1 -right-1 p-1' : '-top-2 -right-2 p-2'}`}>
                                <Download className={isSmall ? "w-3 h-3" : "w-4 h-4 md:w-5 md:h-5"} />
                            </div>
                        )}
                    </div>
                    <span className={textClasses}>
                        {app.name}
                    </span>
                </>
            ) : (
                <span className={placeholderClasses}>
                    {app.name}
                </span>
            )}
        </div>
    );
};
