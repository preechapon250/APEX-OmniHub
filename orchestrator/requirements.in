# Runtime dependencies for apex-orchestrator
# Mirrors [project].dependencies from pyproject.toml
# Regenerate lock: pip-compile --generate-hashes -o requirements.lock requirements.in

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Temporal.io workflow engine
temporalio>=1.5.0

# Data validation and serialization
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Redis with vector search capabilities
redis[hiredis]>=5.0.0
redis-om>=0.2.0

# LLM interface
instructor>=1.0.0
litellm>=1.0.0

# Database
supabase>=2.3.0
asyncpg>=0.29.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.26.0
tenacity>=8.2.0

# Embeddings for semantic cache
sentence-transformers>=2.3.0
numpy>=1.26.0
