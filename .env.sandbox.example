# ============================================================================
# SANDBOX ENVIRONMENT CONFIGURATION
# ============================================================================
# Purpose: Isolated Supabase environment for demo mode
#
# SETUP INSTRUCTIONS:
# 1. Create separate Supabase project for sandbox (or use local instance)
# 2. Apply all migrations from supabase/migrations/ to sandbox
# 3. Seed with demo data using supabase-sandbox/seed.sql
# 4. Copy this file to .env.sandbox and fill in your sandbox credentials
# 5. Ensure sandbox project has RLS policies that allow demo users
#
# SECURITY NOTES:
# - Sandbox should be completely isolated from production
# - Use separate database, separate auth, separate storage
# - Demo data should never contain real PII or sensitive information
# - Sandbox should have rate limiting to prevent abuse
# ============================================================================

# Sandbox Supabase Project URL
VITE_SUPABASE_SANDBOX_URL=http://localhost:54321

# Sandbox Supabase Publishable (Anon) Key
VITE_SUPABASE_SANDBOX_KEY=your-sandbox-anon-key-here

# Optional: Sandbox Service Role Key (for seeding/admin operations)
SUPABASE_SANDBOX_SERVICE_KEY=your-sandbox-service-role-key-here

# Demo Mode Flag (auto-detected from omnidash_settings.demo_mode in runtime)
# This is just for reference - actual toggle is per-user in database
DEMO_MODE_ENABLED=true

# ============================================================================
# VERIFICATION CHECKLIST
# ============================================================================
# [ ] Sandbox Supabase project created
# [ ] All migrations applied to sandbox
# [ ] Demo data seeded
# [ ] RLS policies allow demo user access
# [ ] Environment variables set in deployment (Vercel/Netlify)
# [ ] Tested: Enable demo mode in OmniDash → queries hit sandbox URL
# [ ] Tested: Disable demo mode → queries hit production URL
# [ ] Confirmed: No crosstalk between sandbox and production
# ============================================================================

# ============================================================================
# NFT Verification (verify-nft edge function)
# ============================================================================
# Required even in sandbox — verify-nft always calls the Alchemy API
# Get from: https://dashboard.alchemy.com/ — create sandbox apps per network
ALCHEMY_API_KEY_ETH=your-sandbox-ethereum-alchemy-key
ALCHEMY_API_KEY_POLYGON=your-sandbox-polygon-alchemy-key
